<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionModeOverlay">true</item>
        <!-- Restyle global de toutes les AlertDialog (DSL Splitties appcompat) -->
        <item name="alertDialogTheme">@style/YomikataAlertDialog</item>
        <item name="android:alertDialogTheme">@style/YomikataAlertDialog</item>
    </style>

    <!-- AlertDialog harmonisée au Design System v2 (Session dialogs) -->
    <style name="YomikataAlertDialog" parent="ThemeOverlay.AppCompat.Dialog.Alert">
        <!-- Surface bleu nuit + coins radius_xl + bordure standard -->
        <item name="android:windowBackground">@drawable/bg_dialog</item>
        <!-- Titre (DialogTitle utilise textColorPrimary) -->
        <item name="android:textColorPrimary">@color/color_text_primary</item>
        <!-- Message + textes secondaires -->
        <item name="android:textColorSecondary">@color/color_text_secondary</item>
        <!-- Items des dialogs à liste (setItems) -->
        <item name="android:textColorAlertDialogListItem">@color/color_text_primary</item>
        <!-- Couleur d'accent (curseur EditText, surbrillances) -->
        <item name="colorAccent">@color/color_accent</item>
        <!-- Styles de boutons : action principale orange, autres en sourdine -->
        <item name="buttonBarPositiveButtonStyle">@style/YomikataAlertDialog.Button.Positive</item>
        <item name="buttonBarNegativeButtonStyle">@style/YomikataAlertDialog.Button.Muted</item>
        <item name="buttonBarNeutralButtonStyle">@style/YomikataAlertDialog.Button.Muted</item>
    </style>

    <style name="YomikataAlertDialog.Button.Positive" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/color_accent</item>
    </style>

    <style name="YomikataAlertDialog.Button.Muted" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/color_text_muted</item>
    </style>

    <!-- Thème du splash screen système (Android 12+) -->
    <style name="SplashTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/color_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>

    <style name="Toolbar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/colorAccent</item>
    </style>


    <style name="full_screen_dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="menu_labels_style">
        <item name="android:background">@drawable/fab_label_background</item>
        <item name="android:textColor">@color/fab_menu_text_color</item>
    </style>

    <style name="CustomDialog" parent="@style/Theme.AppCompat.DayNight.Dialog">
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
