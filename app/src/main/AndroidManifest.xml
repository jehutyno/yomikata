<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <queries>
        <package android:name="com.google.android.tts" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
        </intent>
<!--        <provider android:authorities="..." />-->
    </queries>

    <application
        android:name="com.jehutyno.yomikata.YomikataZKApplication"
        android:requestLegacyExternalStorage="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:localeConfig="@xml/locale_config"
        android:theme="@style/AppTheme">

        <!-- RGPD : collecte désactivée par défaut tant que l'utilisateur n'a pas donné son
             consentement explicite. Analytics.setConsent(true) la réactive dynamiquement. -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="google_analytics_default_allow_analytics_storage"
            android:value="false" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_hiragana" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent" />

        <service android:name=".util.FileDownloadService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <activity android:name="com.jehutyno.yomikata.screens.quizzes.QuizzesActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="com.jehutyno.yomikata.screens.content.ContentActivity"/>
        <activity android:name="com.jehutyno.yomikata.screens.answers.AnswersActivity"/>

        <activity
            android:name="com.jehutyno.yomikata.screens.quiz.QuizActivity"
            android:windowSoftInputMode="adjustResize"/>
        <activity android:name="com.jehutyno.yomikata.screens.prefs.PrefsActivity"/>
    </application>

</manifest>